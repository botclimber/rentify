export interface ContactResOwner {
    resOwnerId: number,
    userId: number,
    createdAt: string,
    //message?: string
}

export type sub = {
    id?: number,
    email: string,
    createdAt: string
}